interface:
  display_name: "Council Decision Mapping"
  short_description: "Run decision-mapping with a three-model council"
  default_prompt: "Use $council-decision-mapping with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
