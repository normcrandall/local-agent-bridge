interface:
  display_name: "Council Writing Shape"
  short_description: "Run writing-shape with a three-model council"
  default_prompt: "Use $council-writing-shape with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
