interface:
  display_name: "Council Triage"
  short_description: "Run triage with a three-model council"
  default_prompt: "Use $council-triage with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
