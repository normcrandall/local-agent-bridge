interface:
  display_name: "Take the Helm"
  short_description: "Drive parallel issue lanes through a safe merge train"
  default_prompt: "Use $take-the-helm to take ownership of this objective, schedule independent issues into parallel worktree lanes, serialize exact-SHA integration through the bridge merge train, arbitrate conflicts, and continue without asking me unless a true escalation boundary is reached."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
