interface:
  display_name: "Take the Helm"
  short_description: "Drive parallel issue lanes through a safe merge train"
  default_prompt: "Use $take-the-helm to own this objective end to end. Continuously schedule safe issue lanes, automatically deliver and formally review every PR, repair findings, merge authorized exact heads, and dispatch the next frontier without asking me to continue unless a true escalation boundary is reached."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
