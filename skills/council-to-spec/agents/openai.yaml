interface:
  display_name: "Council To Spec"
  short_description: "Run to-spec with a three-model council"
  default_prompt: "Use $council-to-spec with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
