interface:
  display_name: "Council Edit Article"
  short_description: "Run edit-article with a three-model council"
  default_prompt: "Use $council-edit-article with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
