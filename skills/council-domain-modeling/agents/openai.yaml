interface:
  display_name: "Council Domain Modeling"
  short_description: "Run domain-modeling with a three-model council"
  default_prompt: "Use $council-domain-modeling with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
