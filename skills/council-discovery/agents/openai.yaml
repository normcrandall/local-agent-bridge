interface:
  display_name: "Council Discovery"
  short_description: "Find features through web-wide market discovery"
  default_prompt: "Use $council-discovery to scan the competitive web landscape and publish app enhancements grounded in product, customer, business, and current market evidence."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
    - type: "mcp"
      value: "playwright"
      description: "Isolated browser for public competitive-product research"
policy:
  allow_implicit_invocation: true
