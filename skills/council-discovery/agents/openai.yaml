interface:
  display_name: "Council Discovery"
  short_description: "Find and issue app features with three models"
  default_prompt: "Use $council-discovery to discover and publish the best next features for this app."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
