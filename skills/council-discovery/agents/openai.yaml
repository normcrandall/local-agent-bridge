interface:
  display_name: "Council Discovery"
  short_description: "Find business-grounded features with three models"
  default_prompt: "Use $council-discovery to find and publish app enhancements grounded in retention, acquisition, maintainability, overhead, and ROI."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
