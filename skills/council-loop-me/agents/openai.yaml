interface:
  display_name: "Council Loop Me"
  short_description: "Run loop-me with a three-model council"
  default_prompt: "Use $council-loop-me with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
