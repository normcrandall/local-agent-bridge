interface:
  display_name: "Council Prototype"
  short_description: "Run prototype with a three-model council"
  default_prompt: "Use $council-prototype with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
