interface:
  display_name: "Council To Questionnaire"
  short_description: "Run to-questionnaire with a three-model council"
  default_prompt: "Use $council-to-questionnaire with Claude, Codex, and Antigravity for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "collaboration"
      description: "Persistent Claude, Codex, and Antigravity broker"
policy:
  allow_implicit_invocation: true
